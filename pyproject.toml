[tool.poetry]
name = "aikido-firewall"
version = "0.1.0"
description = "Aikido RASP for Python"
authors = ["Aikido"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
importhook = "^1.0.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
