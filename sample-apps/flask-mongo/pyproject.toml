[project]
name = "flask-mongo"
version = "0.1.0"
description = "Sample app Flask Mongo"
requires-python = ">3.9.1,<3.15"
dependencies = [
    "flask (==3.1.3)",
    "requests (>=2.32.3,<3.0.0)",
    "cryptography (>=44.0.0,<45.0.0)",
    "aikido_zen",
    "flask-pymongo (>=2.3.0,<3.0.0)",
    "ddtrace (>=3.16.0,<4.0.0)",
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
aikido_zen = { path = "../../", develop = true }
